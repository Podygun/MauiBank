<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiBank.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:MauiBank.View"
    FlyoutHeaderBehavior="CollapseOnScroll"
    Shell.FlyoutBehavior="Disabled">


    <FlyoutItem>
        <ShellContent
            Title="main"
            ContentTemplate="{DataTemplate view:MainPage}"
            Route="MainPage" />
        <ShellContent
            Title="auth"
            ContentTemplate="{DataTemplate view:AuthPage}"
            Route="AuthPage" />
        <ShellContent
            Title="reg"
            ContentTemplate="{DataTemplate view:RegPage}"
            Route="RegPage" />
    </FlyoutItem>

    <!--<Tab Title="Tab" Icon="user.png">
            <ShellContent
                Title="main"
                ContentTemplate="{DataTemplate view:MainPage}"
                Route="MainPage" />
            <ShellContent
                Title="auth"
                ContentTemplate="{DataTemplate view:AuthPage}"
                Route="AuthPage" />
            <ShellContent
                Title="reg"
                ContentTemplate="{DataTemplate view:RegPage}"
                Route="RegPage" />
        </Tab>-->




</Shell>
